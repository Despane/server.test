server {
    listen 80;
    listen [::]:80;

    server_name bitcoder.ru www.bitcoder.ru;
    server_tokens off;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://bitcoder.ru$request_uri;
    }
}